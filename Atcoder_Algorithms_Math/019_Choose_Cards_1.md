## 説明（組合せ)

同じ値が **k 個**あるとします。  
そこから **異なる 2 個**を選ぶ方法の数は **組合せ**で表せます。

- 1個目の選び方：**k 通り**
- 2個目の選び方：残り **k-1 通り**

したがって一度は **k * (k-1)** 通りになります。

しかし、  
「A を先に選んで B」 と 「B を先に選んで A」 は **同じ選び方**なので、  
この数え方だと **2回ずつ数えてしまう（重複）** ことになります。

よって、重複分の **2** で割って：kC2 = k*(k-1)/2
